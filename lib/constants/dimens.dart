import 'package:flutter/cupertino.dart';

class Dimens {
  const Dimens._();

  static const brc8 = BorderRadius.all(Radius.circular(8.0));
  static const brc16 = BorderRadius.all(Radius.circular(16.0));
}
